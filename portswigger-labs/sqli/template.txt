Goal: Show you understand the why, not just the what. Keep it tight — 200–300 words. -- Your goal = the "what + where + why it matters."


Lab: [Lab name exactly as PortSwigger titles it]
Level: Apprentice
Vulnerability Class: SQL Injection
Lab URL: [PortSwigger lab link]
Date Completed: [Date]

What the Lab Is Testing
[One sentence. What specific SQLi concept does this lab isolate?]

Example: "Tests whether unsanitized user input in a WHERE clause can be manipulated to bypass conditional logic."


The Vulnerable Behavior
[Describe what the app does and where the vulnerability lives — 2–3 sentences. No exploit yet.]

Example: "The product category filter passes user input directly into a SQL WHERE clause. The app does not sanitize or parameterize the input before executing the query."


My Approach
[What did you try? What worked? Be specific about the payload and why you chose it.]
Payload used:
sql[your payload here]
What the injected query became:
sql[reconstructed query showing how your input changed it]

Why It Works
[Explain the mechanics in plain English. Pretend you're explaining to a developer who doesn't know SQLi.]

The Fix
[What should the developer have done? Be specific to the language/framework if possible.]
java// Example fix in Java (Spring Boot / JDBC)
String query = "SELECT * FROM products WHERE category = ?";
PreparedStatement stmt = conn.prepareStatement(query);
stmt.setString(1, userInput);

Real-World Relevance
[This is the line that separates a student from a researcher. Where have you seen or could you see this pattern in production?]

Example: "Legacy PHP and JSP applications frequently use string concatenation in SQL queries. This pattern also appears in poorly configured ORM raw query methods like Spring's JdbcTemplate.queryForList() when used without binding."


Takeaway
[One sentence. What's the single most important thing to remember from this lab?]
